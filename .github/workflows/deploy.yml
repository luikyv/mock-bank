name: Build and Publish Lambda to AWS S3

permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy-s3:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.21"

      - name: Build Lambda binary
        run: |
          mkdir -p build
          GOOS=linux GOARCH=amd64 go build -o build/bootstrap ./cmd/lambda

      - name: Zip Lambda
        run: |
          cd build
          zip main.zip bootstrap

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:iam::208072937240:role/mockbank-github-oidc
          role-session-name: mockbank-github-ci
          aws-region: us-east-1

      - name: Upload Lambda to S3
        run: |
          aws s3 cp build/main.zip s3://mockbank-lambda-artifacts/mockbank/main.zip
